name: flutter_application_1
description: A new Flutter project.

publish_to: 'none' # Remove this line if you plan to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.0.0 # Asegúrate de usar la versión más reciente
  ionicons: ^0.2.2
  shared_preferences: ^2.0.13
  intl: ^0.19.0 # O la última versión disponible
  css_colors: ^1.1.5
  flutter_calendar_carousel: ^2.4.4 # O la última versión disponible
  flutter_launcher_icons: ^0.13.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0 # Asegúrate de usar la última versión

flutter_launcher_icons:
    image_path: "assets/Pencil.png"
    android: true
    ios: true
    # refactor_android: true
    # refactor_ios: true
    # refactor_web: true
    # web: true
